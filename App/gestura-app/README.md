# GESTURA

Windows desktop app for gesture control using Electron + React + Vite + Tailwind.

Scripts:
- `npm run dev` - Runs Vite dev server and Electron
- `npm run build` - Builds frontend
- `npm run package` - Builds Windows installer via electron-builder

Backend API base URL: `http://localhost:8000`



